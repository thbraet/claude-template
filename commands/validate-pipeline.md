---
name: validate-pipeline
description: "End-to-end smoke test of the data pipeline: raw → clean → features → format"
argument-hint: "[optional: 'train', 'test', or 'both' (default: both)]"
allowed-tools: Skill(validate-pipeline)
---

Invoke the validate-pipeline skill for: $ARGUMENTS
